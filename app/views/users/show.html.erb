<h1> <%= @user.name %> </h1>
<div class='content-wrapper'> 
    <h2> Your Comments: </h2>
    <ul>
    <% @user.comments.each do |comment| %><br>
      <li> <h2><a href="/comments/<%= comment.id %>"><%= comment.title %></a></h2>
    <%end%>
    </li>
</ul>
</div>
<br>

<%= link_to 'Edit User', edit_user_path %>
